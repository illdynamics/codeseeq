ok so can you extend Codeseeq in a way so that we can use this reasoning capabilities of deepseek?

like add a command switch to make it possible to use -R low/high/max and if set, the right reasoning mode on deepseek side will be used?

and maybe set default reasoning in the config, and only to be used with deepseek-v4-flash, deepseek-v4-pro, and thinking versions? (or double check the documentation if we need to use it at thinking versions as well or it automatically does something or we actually do not need to set it)
